package  {	import flash.display.Sprite;	import models.ClockModel;	import views.DigitalView;	import views.AnalogView;	import views.ControlsView;	import controllers.ClockController;	import flash.media.Sound;	import flash.media.SoundChannel;		public class MainClass extends Sprite {		public function MainClass() {						var clockModel:ClockModel = new ClockModel();			var clockController:ClockController = new ClockController(clockModel);						var digital:DigitalView = new DigitalView(clockModel);			digital.x = 400;			digital.y = 200;			addChild(digital);						var analog:AnalogView = new AnalogView(clockModel);			analog.x = 190;			analog.y = 500;			addChild(analog);						var controls:ControlsView = new ControlsView(clockController,clockModel);			controls.x = 547;			controls.y = 499;			addChild(controls);		}	}	}